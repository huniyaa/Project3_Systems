{
  "version": 2,
  "buildCommand": "npx prisma generate",
  "rewrites":[
    {
      "source": "/api/(.*)",
      "destination": "/api/$1"
    },
    {
      "source": "/(.*)",
      "destination":"/"
    }
  ]
}
